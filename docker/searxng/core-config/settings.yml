use_default_settings: true

general:
  instance_name: "Epsilon Search"

search:
  formats:
    - html
    - json

server:
  secret_key: "smileyv2protectepsilonpassword"
  limiter: false
  image_proxy: true